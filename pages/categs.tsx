import * as React from 'react';
import CategPage from '@components/Pages/Categoria';
export interface IAppProps {
}

export default function Categs(props: IAppProps) {
    return (
        <CategPage />
    );
}
